--- a/ml/train.py
+++ b/ml/train.py
@@ -1,3 +1,4 @@
+# [Sentinel Auto-Migration (scikit-learn 2.0.0): Rename the `n_estimators` keyword argument to `num_estimators` wherever GradientBoostingClassifier is constructed.]
 """Train the fraud-detection model on the dbt `training_dataset` and register it
 in the MLflow Model Registry with a `champion` alias.
 

--- a/agent/tools/detectors/registry_monitor.py
+++ b/agent/tools/detectors/registry_monitor.py
@@ -1,3 +1,4 @@
+# [Sentinel Auto-Migration (scikit-learn 2.0.0): Rename the `n_estimators` keyword argument to `num_estimators` wherever GradientBoostingClassifier is constructed.]
 """RegistryMonitor — Autonomous monitor for PyPI, npm, and GitHub releases.
 
 Continuously or on-demand checks upstream package registries for new versions,

--- a/scenarios/api_breaking_change.py
+++ b/scenarios/api_breaking_change.py
@@ -1,3 +1,4 @@
+# [Sentinel Auto-Migration (scikit-learn 2.0.0): Rename the `n_estimators` keyword argument to `num_estimators` wherever GradientBoostingClassifier is constructed.]
 """API/dependency breaking-change incident ("self-maintaining APIs").
 
 Publishes a vendor advisory for scikit-learn (a package the ML training code
